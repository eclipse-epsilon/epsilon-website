@namespace(uri="pdl", prefix="")
package pdl;

class Project {
	attr String name;
	val Deliverable[*] deliverables;
}

class Deliverable {
	attr String name;
	attr int due;
	ref Person lead;
}

class Person {
	attr String name;
}